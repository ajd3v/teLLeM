[package]
name = "tellem-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "teLLeM wasm bindings for browser-side lint and fix"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
tellem-core = { path = "../tellem-core" }
wasm-bindgen = "0.2"
serde_json = "1"
toml = "0.8"
