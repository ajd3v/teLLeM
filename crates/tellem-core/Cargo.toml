[package]
name = "tellem-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "AI-text forensics with receipts: rules, scoring, deterministic fixes"

[dependencies]
aho-corasick = "1"
regex = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "lint"
harness = false
